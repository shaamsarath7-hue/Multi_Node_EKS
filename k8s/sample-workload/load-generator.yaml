apiVersion: apps/v1
kind: Deployment
metadata:
  name: load-generator
  namespace: workloads
spec:
  replicas: 6
  selector:
    matchLabels:
      app: load-generator
  template:
    metadata:
      labels:
        app: load-generator
    spec:
      serviceAccountName: workload-sa
      containers:
        - name: load-generator
          image: busybox:1.36
          command:
            - /bin/sh
            - -c
            - "while true; do wget -q -O- http://php-apache.workloads.svc.cluster.local > /dev/null; done"
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
      nodeSelector:
        nodegroup-type: workload
